[project]
name = "occupation-classifier-api"
version = "0.1.0"
requires-python = "==3.13.14"
dependencies = [
    "fastapi[standard]",
    "tensorflow",
    "numpy",
    "opencv-python-headless",
    "python-multipart",
]

[tool.fastapi]
entrypoint = "api.api:app"